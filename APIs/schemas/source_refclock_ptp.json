{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "description": "Describes a Source with a PTP refclock",
  "title": "Source resource with a PTP refclock",
  "required": [
    "refclock"
  ],
  "properties": {
    "refclock": {
      "description": "Signalling for the reference clock used by the Source",
      "type": "object",
      "required": [
        "type",
        "traceable",
        "version",
        "gmid",
        "locked"
      ],
      "properties": {
        "type": {
          "description": "Type of reference clock used by this Source",
          "type": "string",
          "enum": [
            "ptp"
          ]
        },
        "traceable": {
          "description": "Refclock is synchronised to International Atomic Time (TAI)",
          "type": "boolean"
        },
        "version": {
          "description": "Version of a PTP reference clock used by this Source",
          "type": "string",
          "enum": [
            "IEEE1588-2008"
          ]
        },
        "gmid": {
          "description": "ID of a PTP reference clock used by this Source",
          "type": "string",
          "pattern": "^[0-9A-F]{2}-[0-9A-F]{2}-[0-9A-F]{2}-[0-9A-F]{2}-[0-9A-F]{2}-[0-9A-F]{2}-[0-9A-F]{2}-[0-9A-F]{2}$"
        },
        "locked": {
          "description": "Lock state of this Source to the registered gmid. if true this Source is slaved to this gm, otherwise it has no defined relationship to this gm",
          "type": "boolean"
        }
      }
    }
  }
}
